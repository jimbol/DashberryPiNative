// import { TOGGLE_LIST } from '../constants/action_types'

const initialState = 'my-chores';

export default function chores(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}
